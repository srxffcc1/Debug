<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ssicosm.slime_great_war">
    <uses-permission android:name="android.permission.INTERNET"/>

    <supports-screens android:anyDensity="true" android:largeScreens="true" android:normalScreens="true" android:smallScreens="true"/>

    <!--往sdcard中写入数据的权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <!--在sdcard中创建/删除文件的权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"></uses-permission>

    <application
        android:largeHeap="true"
        android:allowBackup="true"
        android:debuggable="false"
        android:name="net.hacks.ListenerApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        tools:replace="android:icon"
        >
        <meta-data android:name="loadclass" android:value="com.unity3d.player.UnityPlayerActivity">
        </meta-data>
        <activity android:name="net.UnityPlayerActivity" android:screenOrientation="landscape"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <!--<data android:host="UnityPlayerActivity" android:scheme="com.unity3d.player"/>-->
            </intent-filter>

        </activity>
        <activity android:name="net.hacks.WebActivity" android:screenOrientation="portrait" android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

        </activity>

        <activity android:name="net.hacks.ProgressActivity" android:screenOrientation="landscape" android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->

                <!--<category android:name="android.intent.category.LAUNCHER" />-->
                <!--&lt;!&ndash;<data android:host="UnityPlayerActivity" android:scheme="com.unity3d.player"/>&ndash;&gt;-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name="net.hacks.SplashActivity" android:screenOrientation="landscape" android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

        </activity>
    </application>
    <uses-permission
        android:name="com.tencent.mm.permission.GET_QRCODE_INFO"/>
</manifest>